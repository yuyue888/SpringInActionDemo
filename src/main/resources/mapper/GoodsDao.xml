<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="core.dao.GoodsDao">
    <!--auto generated Code-->
    <resultMap id="BaseResultMap" type="core.entity.Goods">
        <result column="gid" property="gid"/>
        <result column="gname" property="gname"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="all_column">
        `gid`,
        `gname`
    </sql>

    <!--auto generated Code-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="goods.gid" parameterType="core.entity.Goods">
        INSERT INTO goods (
            `gid`,
            `gname`
        ) VALUES (
            #{gid},
            #{gname}
        )
    </insert>

    <select id="getAllGoods" resultMap="BaseResultMap">
        SELECT * from goods ;
    </select>

    <!--auto generated Code-->
    <update id="update" parameterType="core.entity.Goods">
        UPDATE goods
        <set>
            <if test="goods.gid != null">`gid` = #{goods.gid},</if>
            <if test="goods.gname != null">`gname` = #{goods.gname}</if>
        </set>
        WHERE gid = #{goods.gid}
    </update>
    <select id="getTotalCount" resultType="int">
      select count(*) from goods;
    </select>
    <select id="findByName" resultMap="BaseResultMap">
      select <include refid="all_column"></include> from goods where gname like #{"%"+name+"%" ,jdbcType=VARCHAR}
    </select>
</mapper>

